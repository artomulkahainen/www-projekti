# WWW-Projekti

## How to run development setup

1. Install Docker and Docker Compose.
2. Run "docker compose up" in the root folder to start the development environment.
3. You should find the website running locally now in: http://localhost:3000

## To keep code pretty

- Install VSCode Extension "Prettier - Code Formatter" (esbenp.prettier-vscode)
